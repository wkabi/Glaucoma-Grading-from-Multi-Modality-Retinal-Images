name: twostream_softce
weight: [1.0, 1.0]
alpha: 0.01
max_alpha: 10.0
temp: 1.0
step_size: 50
step_factor: 10
soft_dist: "non-uniform"
soft_alpha: 0.1
soft_std: 0.5

object:
  _target_: optic.modeling.TwostreamSoftCELoss
  weight: ${loss.weight}
  alpha: ${loss.alpha}
  max_alpha: ${loss.max_alpha}
  temp: ${loss.temp}
  step_size: ${loss.step_size}
  step_factor: ${loss.step_factor}
  soft_dist: ${loss.soft_dist}
  soft_alpha: ${loss.soft_alpha}
  soft_std: ${loss.soft_std}