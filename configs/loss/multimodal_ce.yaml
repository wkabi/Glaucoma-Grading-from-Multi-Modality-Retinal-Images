name: multimodal_ce
weight: [1.0, 1.0]
alpha: 0.01
max_alpha: 10.0
temp: 1.0
step_size: 50
step_factor: 10

object:
  _target_: optic.modeling.MultimodalCELoss
  weight: ${loss.weight}
  alpha: ${loss.alpha}
  max_alpha: ${loss.max_alpha}
  temp: ${loss.temp}
  step_size: ${loss.step_size}
  step_factor: ${loss.step_factor}